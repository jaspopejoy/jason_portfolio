document.addEventListener("DOMContentLoaded", function () {
    var options = {
        strings: ["Full Stack Developer", "Web Developer", "IT Specialist", "Creative Problem Solver"],
        typeSpeed: 60,
        backSpeed: 40,
        backDelay: 1000,
        startDelay: 500,
        loop: true,
    };

    var typed = new Typed(".typed", options);
});
